This white screen is happening only in Replit Preview after importing the repo fresh from GitHub. This is not a published deployment issue yet.

The app worked in the original Replit, was pushed to GitHub, then imported into a fresh Replit. The fresh Replit preview starts with a white screen.

Do not add new features. Only fix the fresh Replit preview startup issue.

Important:
A working Replit environment does not fully transfer through GitHub. GitHub has the code, but the fresh Replit still needs dependencies installed, database/file fallback handled, the API started, the frontend started, and the preview opened on the correct port.

Port rules:
3001 = backend/API server.
5000 = frontend website/admin app.
The preview/webview must open port 5000, not 3001.

Diagnose preview startup specifically:
1. Check Replit console for the API Server workflow.
2. Check Replit console for the Start application/frontend workflow.
3. Check browser console in the preview iframe.
4. Confirm Preview is using port 5000.
5. Confirm API is running on 3001.
6. Confirm frontend is running on 5000.
7. Confirm /api/cc/bootstrap works through the frontend proxy.
8. Confirm pnpm install ran in this fresh Replit.
9. Confirm DATABASE_URL exists or file storage fallback is being used.
10. Confirm no frontend runtime crash is happening after the JS bundle loads.

Fix .replit preview workflow:
The Project run button should not just start the API and frontend directly. It should run a safe startup process first.

Update the preview/startup flow so it:
1. Runs pnpm install.
2. If DATABASE_URL exists, runs pnpm --filter @workspace/db run push.
3. If DATABASE_URL is missing, prints “DATABASE_URL not set; using file storage fallback” and continues.
4. Starts API server on port 3001.
5. Waits briefly for API startup.
6. Starts frontend on port 5000 with API_PORT=3001.
7. Opens the webview/preview on port 5000.

Also update scripts/replit-start.sh:
- Run pnpm install.
- Only run database push if DATABASE_URL exists.
- Do not fail when DATABASE_URL is missing.
- Start API on 3001.
- Start frontend on 5000 with API_PORT=3001.

Then wire .replit Project workflow to use that safe startup script or equivalent safe startup command.

Manual checks to run:
pnpm install

if [ -n "$DATABASE_URL" ]; then pnpm --filter @workspace/db run push; else echo "DATABASE_URL not set; using file storage fallback"; fi

curl -i http://127.0.0.1:3001/api/cc/bootstrap | head -40
curl -i http://127.0.0.1:5000/api/cc/bootstrap | head -40
curl -i http://127.0.0.1:5000/ | head -40

Specific builds:
pnpm --filter @workspace/candy-crackzzz run build
pnpm --filter @workspace/api-server run build

Success criteria:
- Replit Preview loads home page.
- /menu loads.
- /merch loads.
- /admin/login loads.
- /api/cc/bootstrap returns 200 through port 5000 proxy.
- Preview is confirmed to be port 5000.
- Port 3001 is only used for API/backend.
- No blank white screen.
- No red browser console runtime crash.

Final report required:
Once the error is found, explain exactly what caused the error and exactly what was done to fix it.

Include:
1. Exact root cause.
2. Whether it was preview port, install, database, API, proxy, frontend runtime, cache/localStorage, or workflow related.
3. Exact console/log error.
4. Exact file/config that caused it.
5. Exact files changed.
6. Exact commands/tests run.
7. Result of each test.
8. Confirmation this will work on a fresh Replit import.
9. Any manual steps I still need to do.